export default class Comment {
    constructor (name, body) {
        this.id = id,
        this.name = name,
        this.body = body
    }

    show() {
        console.log(`my name is ${this.name} and my body  is ${this.body}`)
    }
}